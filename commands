this file has git command and their description
  at first open command prompt and 
1- get git version: git --version
2- install git : opt-get install git in windows
		 brew install git    in mac
3- clone git repository: git clone Address
4- see files status and if there is change to push(red file is untracked files for changes):
   git status 
5- add untracked files: git add fileaddress(untracked file path).it convert file status to green in git status
6- commit file's changes with message: git commit -m 'file added for git command desc'
7- push to repository: git push 
note: master branch is default branch and main
8- get file changes : git diff README.md (shows changes and added line (and removed lined in red)) 
9- git log (to see all changes log)
10- git checkout README.md (remove latest change in readme.md)




گام‌های اصلی:

مرحله ۱ – Git init کن داخل روت پروژه

cd مسیر_پروژه
git init

مرحله ۲ – فایل‌ها رو اضافه کن و Commit بزن

git add .
git commit -m "initial commit for VoiceTranslator project"

مرحله ۳ – برو تو سایت github.com (https://github.com/) و یه ریپازیتوری جدید بساز
 • بدون README
 • فقط یه اسم بده، مثلاً: VoiceTranslator

مرحله ۴ – پروژه رو به GitHub وصل کن

git remote add origin https://github.com/username/VoiceTranslator.git
git branch -M main
git push -u origin main

به‌جای username، یوزرنیم GitHub خودتو بذار.

